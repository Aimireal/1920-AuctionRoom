/*
    RemoveLotGUI: Allows a user to remove their lot from auction given parameters are met (ID, no bids and active)
    ToDo: Remove logic. Best left until creation is sorted
 */

import net.jini.core.lease.*;
import net.jini.core.transaction.Transaction;
import net.jini.core.transaction.TransactionFactory;
import net.jini.core.transaction.server.TransactionManager;
import net.jini.space.JavaSpace;

public class RemoveLotGUI
{
    public static void removeLot(String lot, JavaSpace js, TransactionManager tranMan)
    {

    }

}
